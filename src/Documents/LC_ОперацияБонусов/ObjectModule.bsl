////////////////////////////////////////////////////////////////////////////////
// Документ LC_ОперацияБонусов — модуль об'єкта
// Службовий документ-реєстратор рухів регламентних операцій
// (активація, згоряння бонусів).
////////////////////////////////////////////////////////////////////////////////

#Область ОбработчикиСобытий

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	// Рухи формуються програмно в LC_БонуснаяПрограммаСервер
	// перед викликом Записать(Проведение).
	// Тут лише контроль наявності рухів.
	
	Если Движения.LC_БонусныеБаллыКлиентов.Количество() = 0 Тогда
		Отказ = Истина;
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработкаУдаленияПроведения(Отказ)
	
	// Сторно регламентних операцій не передбачено —
	// активація/згоряння є фінальними.
	
КонецПроцедуры

#КонецОбласти
